import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'

// https://vitejs.dev/config/
export default {
  build: {
    outDir: 'dist', // Change this to '.' or 'public' or any desired folder
  },
};
